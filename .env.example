# OpenRouter (https://openrouter.ai)
OPENROUTER_API_KEY=
# Retry transient OpenRouter/provider errors (HTTP 429/5xx, provider 524 etc.)
MANIM_VIDEO_GEN_OPENROUTER_RETRIES=2
MANIM_VIDEO_GEN_OPENROUTER_RETRY_BASE_SECONDS=1.5
MANIM_VIDEO_GEN_OPENROUTER_RETRY_MAX_SECONDS=15

# When the model returns JSON that fails to parse or match schema, retry up to this many
# completion calls (each is a new OpenRouter request). Default 3 = initial + 2 retries.
MANIM_VIDEO_GEN_LLM_JSON_PARSE_MAX_ATTEMPTS=3

# ElevenLabs (https://elevenlabs.io)
ELEVENLABS_API_KEY=
# Voice ID — 한국어 품질을 위해 반드시 설정 권장
#
# 무료 플랜: ElevenLabs 대시보드 Voice Lab에서 직접 만든 보이스의 ID만 API로 사용 가능
# (라이브러리 프리셋 음성은 무료 플랜에서 API 호출 불가 → 402 오류)
#
# 한국어 지원 multilingual_v2 음성 예시 (Voice Library에서 확인):
#   Aria (기본값)  : 9BWtsMINqrJLrRacOk9x
#   Charlotte      : XB0fDUnXU5powFXDhCwa
#   Sarah          : EXAVITQu4vr4xnSDxMaL
#
# 최상의 한국어 품질: https://elevenlabs.io/voice-library 에서
# "Korean" 필터로 전용 한국어 음성을 검색하여 ID를 복사하세요.
ELEVENLABS_VOICE_ID=
# Try /with-timestamps first (falls back to standard TTS on 402/403). Set false to skip.
MANIM_VIDEO_GEN_ELEVENLABS_TRY_TIMESTAMPS=true

# LLM models (OpenRouter slugs)
MANIM_VIDEO_GEN_MODEL_SOLVE=openai/gpt-4o
MANIM_VIDEO_GEN_MODEL_SCRIPT=openai/gpt-4o
MANIM_VIDEO_GEN_MODEL_MANIM=openai/gpt-4o

# Manim render quality: l, m, h, p, k
MANIM_VIDEO_GEN_MANIM_QUALITY_LOW=l
MANIM_VIDEO_GEN_MANIM_QUALITY_HIGH=h

# FFmpeg crossfade between segments (seconds)
MANIM_VIDEO_GEN_CROSSFADE_DURATION=0.25

# Semantic bridge transition between adjacent scenes/chains (true/false)
# If mapping confidence is low, pipeline falls back to normal hard cut.
MANIM_VIDEO_GEN_SCENE_BRIDGE_ENABLED=true

# TTS: elevenlabs | azure | replicate
MANIM_VIDEO_GEN_TTS_PROVIDER=elevenlabs
# Azure Speech (when MANIM_VIDEO_GEN_TTS_PROVIDER=azure)
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=
MANIM_VIDEO_GEN_AZURE_TTS_VOICE=ko-KR-SunHiNeural

# Replicate qwen/qwen3-tts (when MANIM_VIDEO_GEN_TTS_PROVIDER=replicate)
# https://replicate.com/qwen/qwen3-tts
REPLICATE_API_TOKEN=
# custom_voice | voice_clone | voice_design
MANIM_VIDEO_GEN_REPLICATE_TTS_MODE=custom_voice
MANIM_VIDEO_GEN_REPLICATE_TTS_SPEAKER=Aiden
MANIM_VIDEO_GEN_REPLICATE_TTS_LANGUAGE=auto
# voice_design: natural language voice description
MANIM_VIDEO_GEN_REPLICATE_TTS_VOICE_DESC=
# voice_clone: reference audio URL + optional transcript
MANIM_VIDEO_GEN_REPLICATE_TTS_REF_AUDIO=
MANIM_VIDEO_GEN_REPLICATE_TTS_REF_TEXT=
# Optional style (e.g. speak slowly and calmly)
MANIM_VIDEO_GEN_REPLICATE_TTS_STYLE=

# Burn ASS subtitles during segment merge (true/false)
MANIM_VIDEO_GEN_BURN_SUBTITLES=true

# Subtitle tuning (ASS)
# - auto: do NOT inject manual \N; libass wraps by width/margins automatically (recommended)
# - char: inject manual \N using MANIM_VIDEO_GEN_SUBTITLE_MAX_CHARS
MANIM_VIDEO_GEN_SUBTITLE_MAX_CHARS=56
MANIM_VIDEO_GEN_SUBTITLE_WRAP_MODE=auto
# Subtitle font + margins (pixels)
MANIM_VIDEO_GEN_SUBTITLE_FONT_SIZE=42
MANIM_VIDEO_GEN_SUBTITLE_MARGIN_L=56
MANIM_VIDEO_GEN_SUBTITLE_MARGIN_R=56
MANIM_VIDEO_GEN_SUBTITLE_MARGIN_V=44

# Reserve bottom safe-area to avoid subtitle overlap (pixels, 0=off)
# Example: 160 -> keep bottom 160px for subtitles, shrink/pad video above it.
MANIM_VIDEO_GEN_SUBTITLE_SAFE_AREA_PX=0

# narration-visual consistency validation: off | warn | error
# - off: skip validation
# - warn: log issues and continue
# - error: stop run on first error-level issue
MANIM_VIDEO_GEN_CONSISTENCY_MODE=warn

# Auto-repair loop for consistency_mode=error
# - true: when error-level issues are found, ask scriptify to repair JSON and re-validate
# - false: fail immediately on first error-level issue
MANIM_VIDEO_GEN_CONSISTENCY_AUTO_REPAIR=true
# max extra repair attempts (integer >= 0)
MANIM_VIDEO_GEN_CONSISTENCY_AUTO_REPAIR_MAX_ATTEMPTS=2

# Diagnostics
# Save run artifacts (script/segments/generated scene code/ASS/timing) to artifacts/runs/<run_id>/
MANIM_VIDEO_GEN_DIAGNOSTIC_DUMP=false
# Keep .tmp/manim_video_* workspace after run (useful for debugging)
MANIM_VIDEO_GEN_KEEP_WORKSPACE=false

# Optional output size / FPS (0 = Manim default)
MANIM_VIDEO_GEN_VIDEO_WIDTH=0
MANIM_VIDEO_GEN_VIDEO_HEIGHT=0
MANIM_VIDEO_GEN_VIDEO_FPS=0

# Optional background music path (absolute or relative); mixed under narration
MANIM_VIDEO_GEN_BGM_PATH=
MANIM_VIDEO_GEN_BGM_VOLUME=0.2

# 한국어 렌더링 CJK 폰트 (xelatex + xeCJK)
# Windows 기본값: Malgun Gothic / macOS 기본값: AppleGothic / Linux 기본값: Noto Sans CJK KR
# 기본값(OS 자동 감지)을 변경하려면 아래 주석을 해제하고 폰트명을 입력하세요.
# MANIM_VIDEO_GEN_CJK_FONT=Malgun Gothic
