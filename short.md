[제3강 계량경제학 3.1 단순 선형회귀 모형 (The Simple Linear Regression Model)]

■ 회귀분석의 목적
1. y=f(x) 와 같이 이론에서 주어지는 경제적 변수들 간의 관계를 추정(estimate) 및 검정(test).
2. 설명변수의 값을 토대로 종속변수의 값을 예측(forecast)

■ 주당 식료품 지출 예시
- y = 매주 식료품에 지출된 액수($).
- x = 소비자의 주당 소득.
- x가 주어졌을 때 y의 기대값과 x와의 관계는 선형(linear)일 수 있음: E(y|x) = β₁ + β₂x
- Probability Distribution f(y|x=480) of Food Expenditures if given income x=$480 -> 평균: μ_{y|x=480}
- Probability Distribution of Food Expenditures if given income x=$480 and x=$800 -> 평균: μ_{y|x=480}, μ_{y|x=800}
- 평균지출(average expenditure) E(y|x) = β₁ + β₂x
  * β₁: y절편
  * β₂ = ΔE(y|x) / Δx (기울기)
  * The Economic Model: a linear relationship between average expenditure on food and income.

■ 등분산과 이분산
- 등분산(Homoskedastic)의 경우: 주어진 주당소득 X_t (예: x₁=480, x₂=800)에 상관없이 식료품 지출 y_t의 확률밀도함수(PDF)의 퍼진 정도(분산)가 일정함.
- 이분산(Heteroskedastic)의 경우: 주당소득 X_t (x₁, x₂, x₃)가 증가함에 따라 y_t의 분산이 커짐. The variance of y_t increases as household income, X_t, increases.

■ 단순 선형회귀 모형의 가정
1. 주어진 x에 대해 y의 평균값은 선형(회귀)식으로 주어진다. E(y) = β₁ + β₂x, t=1....T
2. 각 x_t의 값에 대해 y의 값은 그 평균값 주변에서 다음의 분산을 갖는 분포를 한다. var(y_t) = σ²
3. y의 값들은 상관되어 있지 않으며, 즉 0의 공분산을 가지며, 따라서 선형관계를 갖지 않는다. cov(y_i, y_j) = 0
4. 변수 x는 적어도 두개의 다른 값을 가지며 확률변수가 아니다.
5. (추정 시 반드시 필요하지는 않은 추가적 선택 가정, optional) y_t의 값들은 각 x값에 대해 그들의 평균 주변에서 정규분포를 함. y_t ~ N[(β₁ + β₂x_t), σ²]

■ 오차항(The Error Term)
- 오차항의 도입: y는 두 부분으로 구성되는 확률변수이다.
  I. 체계적 부분: E(y_t) = β₁ + β₂x_t (y의 평균임)
  II. 확률적 부분: ε_t = y_t - E(y_t) = y_t - β₁ - β₂x_t (이 부분을 오차항이라고 부름)
- 모형 형성: Y_t = β₁ + β₂x_t + ε_t
- 오차항의 원천
  1. 모형에서 구체화 되지 않은 요소들/설명변수들이 오차항에 있을 수 있다.
  2. y와 x의 관계가 정확히 선형관계가 아닌 경우 근사(approximation) 오차가 오차항에 있게 된다.
  3. 관측치에 특정적인 순수하게 예측불가능한 확률적 행태가 오차항에 있게 된다.
- 오차항의 분포: 오차항 f(ε)은 0을 중심으로 분포하며, f(y)는 β₁ + β₂x를 중심으로 분포함. (Probability density function for ε and y)

■ 단순 선형회귀 모형의 가정 II (오차항 기준)
1. x의 각 값에 대해 y의 값은: Y_t = β₁ + β₂x_t + ε_t, t=1,...,T
2. (주어진 x에 대해) 오차항 ε의 평균적 값은: E(ε_t) = 0
3. (주어진 x에 대해) 오차항 ε의 분산은: var(ε_t) = σ² = var(y_t)
4. 임의의 두 오차항 쌍(pair)간의 공분산은: cov(ε_i, ε_j) = cov(y_i, y_j) = 0
5. x는 반드시 두 개의 다른 값을 가지며 확률변수가 아니다.
6. (optional) ε_t는 평균은 0이고 분산은 var(ε_t) = σ²인 정규분포를 한다. ε_t ~ N(0, σ²)

■ 참고: 단순 선형회귀 모형의 가정 III : 강외생성 (strict exogeneity)
- 설명변수 x가 확률변수가 아니라는 가정: 실험실에서의 통제된 자료와 같이 x를 변화시킴에 있어서, y에 영향을 미치는 다른 모든 요인들(ε)은 x의 변화와 무관함을 보증. 이 경우 x의 변화가 y에 대해 미치는 영향의 크기를 ΔE(y_i|x_i)/Δx_i = β₂ 에서 확인할 수 있음.
- 설명변수 x가 확률변수라고 가정해도, x의 변화가 y에 영향을 미치는 다른 모든 요인들과 독립적이라면 동일한 결과를 얻음.
- E(ε_t|x₁, ..., x_T) = 0 를 충족하는 경우 설명변수 x는 강 외생적(strictly exogenous)이라고 함. 이는 설명변수 x가 어떻게 변화하던 무관하게, y에 영향을 미치는 다른 요인들(ε)이 평균적으로 y에 미치는 영향은 항상 0임을 의미. 따라서 x의 변화는 ε의 변화와 독립적임.
- 강 외생성 하의 가정 1:
  1. y_t = β₁ + β₂x_t + ε_t, t=1,...,T
  2. (x_t, y_t)는 확률표본(각 쌍은 동일하고 독립적인 분포)
  3. (주어진 x_t에 대해) E(ε_t|x_t) = 0 (강 외생성)
  4. var(ε_t|x_t) = σ² = var(y_t|x_t)
  5. x는 반드시 두 개의 다른 값을 가진다.
  6. (optional) ε_t|x_t ~ N(0, σ²)
- 강 외생성 하의 가정 2:
  1. y_t = β₁ + β₂x_t + ε_t, t=1,...,T
  2. (주어진 x = (x₁, ..., x_T)에 대해) E(ε_t|x) = 0 (강 외생성)
  3. Var(ε_t|x) = σ² = var(y_t|x)
  4. cov(ε_i, ε_j|x) = cov(y_i, y_j|x) = 0
  5. x는 반드시 두 개의 다른 값을 가진다.
  6. (optional) ε_t|x_t ~ N(0, σ²)

■ 모수의 추정 (Estimating the parameters)
- 모집단(Population) 회귀값: y_t = β₁ + β₂x_t + ε_t
- 모집단 회귀선(regression line): E(y_t|x_t) = β₁ + β₂x_t
- 표본(Sample) 회귀값: y_t = b₁ + b₂x_t + e_t (또는 \hat{\epsilon}_t)
- 표본(Sample) 회귀선: \hat{y}_t = b₁ + b₂x_t
- 관측치 점들과 모집단 회귀선 간의 거리는 오차항(ε), 표본 회귀선 간의 거리는 잔차(\hat{\epsilon})로 나타남.

■ 최소제곱의 원칙 (The Least Squares Principle)
- 편차(잔차)의 제곱의 합을 최소화함. (The sum of squared residuals from any other line will be larger.)
- S(β₁, β₂) = \sum_{t=1}^{T} (y_t - β₁ - β₂x_t)²
- 최소제곱 추정(The Least Squares Estimation): S(β₁, β₂)를 β₁과 β₂에 대해 각각 편미분하여 0으로 놓음.
  * \partial S / \partial β₁ = -2 \sum (y_t - β₁ - β₂x_t) = 0
  * \partial S / \partial β₂ = -2 \sum x_t (y_t - β₁ - β₂x_t) = 0
- 이 두 식을 0으로 놓게 되면 미지수 β₁, β₂를 최소값에 대응되는 특별한 값인 b₁, b₂로 표기함.
  * \sum y_t - T*b₁ - b₂ \sum x_t = 0  =>  T*b₁ + b₂ \sum x_t = \sum y_t
  * \sum x_t*y_t - b₁ \sum x_t - b₂ \sum x_t² = 0  =>  b₁ \sum x_t + b₂ \sum x_t² = \sum x_t*y_t
- 이 정규방정식을 b₁ 과 b₂에 대해서 풀면:
  * b₂ = (T \sum x_t*y_t - \sum x_t \sum y_t) / (T \sum x_t² - (\sum x_t)²)
  * b₁ = \bar{y} - b₂\bar{x}

■ 추정값의 활용 및 탄력성
- 탄력성 개념: η = (Δy/y) / (Δx/x) = (Δy/Δx) * (x/y)
- 미적분학을 이용한 한 점에서의 탄력성: η = lim_{Δx->0} [(Δy/Δx)*(x/y)] = (\partial y / \partial x) * (x/y)
- 선형 모델 E(y) = β₁ + β₂x 에서 \partial E(y) / \partial x = β₂ 이므로, 탄력성 η = β₂ * (x / E(y))
- 표본 평균 (\bar{x}, \bar{y})에서의 탄력성 추정치: \hat{\eta} = b₂ * (\bar{x} / \bar{y})
- 예시: 임금 예측 방정식 \hat{y}_t = 4 + 1.5x_t (x: 경력 년수, y: 시급)
  * 만약 \bar{x} = 8 (평균 경력), \bar{y} = $16 (평균 임금) 이라면, (\bar{x}, \bar{y})에서의 탄력성은 \hat{\eta} = 1.5 * (8 / 16) = 0.75
  * 예측 활용: 경력 x_t = 2년이면 예측 시급 \hat{y}_t = $7.00. 경력 x_t = 3년이면 예측 시급 \hat{y}_t = $8.50.

■ 단순 선형회귀 모형의 다른 형태: Log-log 모형
- 식: ln(y) = β₁ + β₂ln(x)
- 미분: \partial ln(y) / \partial x = β₂ * (\partial ln(x) / \partial x)
  => (1/y) * (\partial y / \partial x) = β₂ * (1/x)
  => (x/y) * (\partial y / \partial x) = β₂
- 따라서 Log-log 모형에서 x에 대한 y의 탄력성 η = β₂ (기울기 계수 자체가 곧바로 탄력성이 됨)

---

[제1강 서론]

■ 강의 개요
- 강의시간: 월, 수요일 12:00-13:15 (중간 및 기말 시험 때만 의미 있음)
- 강의방식: 원격수업(eclass 동영상, 필요시 실시간 zoom) + 대면 시험
- 강의자료 홈페이지: http://econ22.cafe24.com/metric26.html
- 교재: Hill, Griffiths, Lim - Principles of Econometrics, 4th or 5th edition (링크는 eclass 게시)
- 강의 수준: 경제통계학 또는 기초통계학 이수를 전제로 함. 기초 미적분 및 행렬대수 개념을 알고 있으면 좋으나 크게 중요하진 않음.
- 평가 비율: 중간고사 (40%-50%) + 기말고사 (50%-60%) (상황에 따라 변동 가능)
- 학점취득 최소요건: D 이상의 학점은 "중간고사 + 기말고사" 점수가 최소 수준(평균점수의 10%) 이상이어야 부여. 0점은이유불문 F처리.
- 출석: 온라인 강의는 한 학기 동안 오픈하나, 출석 인정 기간은 1주일 부여. 결석이 4회를 초과할 경우 F처리 될 수 있음(출석 자체는 점수 반영 X).
- 과제: 4차례 정도 제시되나 직접 평가 미반영. 시험에 반영하여 간접 평가함. 제출 필요 없고 해답 미제공. GRETL 사용법 자습 기회로 삼음.
- 강의 내용: 중간고사 이전까지 표준적인 선형 회귀 모형 학습, 이후 변형 모형 학습. 기초 이론적 토대에 초점을 맞추며 실증분석은 보조적임. (심도 있는 실증분석은 2학기 응용계량경제학에서 다룸)

■ 계량경제학이란?
- 실제 경제적 현상/관계에 대한 정량적(quantitative) 분석과 측정을 제공하여 경제적 결정을 도움.
- 기초가 되는 세 가지: 1. 경제 이론(변수 간 수학적 관계)  2. 경제 자료(관측치)  3. 통계 모형(관측치들 간의 관계)
- 경제자료의 종류 예시: 
  * 시계열자료(Time-series data): 연도/분기별 연속된 데이터 (예: 1980:1 ~ 1985:4 기간의 CAPITAL 등 관측치)
  * 횡단면 자료(Cross-sectional data) 및 패널 자료(Panel data): 여러 기업(firm)이나 개인의 다년도 데이터
- 계량경제모형 (econometric model): 통계적 모형을 바탕으로 경제 이론과 경제 자료를 결합시키는 것.
  * 경제 이론의 정성적 주장(예: 식료품지출 C는 소득 Y의 함수 C=f(Y) 이며 f'>0 이다)이 타당한지, 그리고 "얼마나" 변화시키는지 정량적으로 분석하기 위해 필요함.

■ 정량적 분석의 필요성 (얼마나?)
- 효과적인 정책 수립을 위해서는 정책수단이 얼마나 변해야 하는지 알아야 함.
- 예: 인플레 방지를 위해 이자율을 얼마나 올려야 하는가? 원/달러 환율 10% 하락이 무역수지에 얼마나 영향을 미치는가? 유가 10% 하락이 소비자물가지수에 얼마나 영향을 미치는가?

■ 통계모형과 오차의 포함
- 실제 소비 지출 C = 체계적 부분 + 확률적 오차(random error, 혹은 오차항 error term) => C = f(Y) + ε
- f(Y)를 구체적으로 선형함수로 정의하면: f(Y) = β₁ + β₂Y
- 통계적 모형식: C = β₁ + β₂Y + ε
- 다른 형태 (수요함수 예시 - Cobb-Douglas 형태): 
  * q^d = A * p^(β₂) * (p^c)^(β₃) * (p^s)^(β₄) * i^(β₅) * ε' (multiplicative 오차 포함)
  * 로그 변환을 통한 통계적 모형: ln q^d = β₁ + β₂ln p + β₃ln p^c + β₄ln p^s + β₅ln i + ε (단, β₁ = ln A, ε = ln ε')

■ 용어 및 표기 (y = β₁ + β₂X₁ + β₃X₂ + ε)
- 좌변 변수 (Left hand-side Variable), y: 종속변수 (Dependent), 설명되는 변수 (Explained), 예측되는 변수 (Predictand), 회귀변수 (Regressand), 반응변수 (Response), 내생변수 (Endogenous)
- 우변 변수 (Right hand-side Variable), X: 설명변수 (Explanatory), 독립변수 (Independent), 예측변수 (Predictor), 회귀변수 (Regressor), 자극 또는 통제변수 (Stimulus or control), 외생변수 (Exogenous)

■ 통제된 실험 vs 통제되지 않은 실험
- 통제된 실험 (controlled, "순수" 과학): 기체의 부피 Y를 설명할 때 압력 X₁을 일정하게 하고 온도 X₂를 변화시키는 실험이 가능함.
- 통제되지 않은 실험 (uncontrolled, 계량경제학): 소비 Y를 설명할 때 가격 X₁과 소득 X₂가 동시에 변함. 소비의 변화가 역으로 가격과 소득을 변화시킬 수도 있음(관측적 자료, observational).

■ 계량경제모형 분석의 단계
1. 추정(Estimation): 관찰할 수 없고(unobservable) 알려지지 않은(unknown) 모수(parameter)들(β₁, β₂, σ²)을 경제적 자료와 오차의 성질에 대한 가정을 바탕으로 적절하게 추정함.
2. 통계적 검정(Testing): 추정 결과를 바탕으로 경제 이론의 주장들을 검정함. (예: 과연 β₂ ≠ 0 인가? 과연 β₂ > 0 인가?)
3. 예측(Forecasting): 모수 추정 결과를 바탕으로 종속변수의 미래값을 예측함. (예: 소득이 Y₀인 가구의 식료품 지출 예측)

■ 계량경제분석(실증분석, 실증연구)의 전체 흐름도
경제이론; 과거의 경험 및 연구 -> 모형 구성: 원인-결과 (C = f(Y) => C_t = β₁ + β₂Y_t + ε_t) -> 자료 취득 (월별, 분기별, 연간 자료 등) -> 모형 추정 (단순 추정법부터 복잡한 방법들) -> 가설 검정 (H₀: β₂ > 0, 한계소비성향이 양인가? 만일 아니라면 모형 재구성으로 피드백) -> 결과 해석 -> 예측 -> 정책적 의미와 결정들 활용.

---

[제2강 통계적 기초]

■ 확률변수 (Random Variable, r.v.)
- 관측되기 전까지는 그 값이 알려지지 않은 변수. 확률적 실험(실제 수행 및 가상적 실험 모두 포함)으로부터 결과됨.
- 확률변수는 모든 가능한 값들의 집합에 대해 정의된 (알려지거나 알려지지 않은) 어떤 확률분포의 존재가 연계됨. 반면, 임의의 일반 변수는 연계된 확률분포를 가지지 않음.

■ 확률변수의 분류
1. 이산확률변수 (Discrete Random Variable): 정수를 이용해서 셀 수 있는 값들을 갖는 확률변수. (예: 복권 상금 결과 - 0원, 1백만원, 1천만원, 1억원 등)
2. 연속확률변수 (Continuous Random Variable): 실선(real line)상의 구간(들)의 실수값들을 갖는 확률변수. (예: GNP, 통화공급량, 이자율, 쌀 가격, 가계소득, 의류 지출 등)
3. 더미변수 (Dummy Variable): 두 개의 가능한 값(대개 0과 1)만을 갖는 이산확률변수 (이원변수, 모의변수). 질적(qualitative) 차이를 나타냄. (예: 성별(0=남성, 1=여성), 고용(0=실업, 1=취업), 거주지(0=비서울, 1=서울), 소득수준(0=저소득, 1=고소득))

■ 확률(밀도)함수 (Probability (Density) Function)
- 이산확률변수: 취하는 모든 가능한 값들에 대해 해당 값의 발생 확률을 대응시켜주는 함수.
  * f(x) = P(X = x), 0 ≤ f(x) ≤ 1
  * n개의 값을 가질 경우: f(x₁) + f(x₂) + ... + f(x_n) = 1. (예: 주사위 눈 X=1~6일 때 각각 f(x)=1/6)
  * 그래프 상에서 특정 값이 취할 확률은 높이(bar)로 나타낼 수 있음.
- 연속확률변수: 특정 하나의 값을 취할 확률은 0임 (P[X=a] = 0). 확률을 나타내기 위해 높이가 아니라 f(x)가 나타내는 곡선 아래의 면적(area)을 이용함. 면적을 갖기 위해서는 X가 취하는 값의 구간이 필요함.
  * P[a < X < b] = \int_{a}^{b} f(x) dx
  * f(x) 그 자체가 아니라 f(x)의 적분이 확률을 정의하며, 이때 f(x)를 연속확률변수 X의 확률밀도함수(pdf)라고 함.

■ 누적분포함수 (Cumulative Distribution Function, cdf)
- 정의: F(x) ≡ P[X ≤ x]
- 이산적 r.v.: F(x) = \sum_{x_i ≤ x} f(x_i)  (계단함수 형태, step function)
- 연속적 r.v.: F(x) = \int_{-∞}^{x} f(x) dx  (연속함수 형태)
- 특징: 모든 확률변수에 대해 cdf는 존재하지만, pdf가 존재하지 않는 확률변수도 있음. 누적분포함수는 감소하지 않는 함수(non-decreasing)이며, 우측연속(right continuous)임. 0에서 1 사이의 값을 가짐.

■ 합산법칙 (Rule of Summation)
- Rule 1: \sum_{i=1}^{n} x_i = x₁ + x₂ + ... + x_n
- Rule 2: \sum_{i=1}^{n} a*x_i = a \sum_{i=1}^{n} x_i
- Rule 3: \sum_{i=1}^{n} (x_i + y_i) = \sum_{i=1}^{n} x_i + \sum_{i=1}^{n} y_i (∑는 선형작용자 linear operator 임을 의미)
- Rule 4: \sum_{i=1}^{n} (a*x_i + b*y_i) = a \sum_{i=1}^{n} x_i + b \sum_{i=1}^{n} y_i
- Rule 5 (표본평균의 정의): \bar{x} ≡ (1/n) \sum_{i=1}^{n} x_i  =>  \sum_{i=1}^{n} (x_i - \bar{x}) = 0
- Rule 6: \sum_{i=1}^{n} f(x_i) = f(x₁) + f(x₂) + ... + f(x_n)  (표기: \sum_{x} f(x_i) = \sum_{i} f(x_i))
- Rule 7 (이중합산): \sum_{i=1}^{n} \sum_{j=1}^{m} f(x_i, y_j) = \sum_{i=1}^{n} [f(x_i, y₁) + ... + f(x_i, y_m)]
  * 합산의 순서는 상관없음: \sum_{i=1}^{n} \sum_{j=1}^{m} f(x_i, y_j) = \sum_{j=1}^{m} \sum_{i=1}^{n} f(x_i, y_j)

■ 기대값 (Expected Value)
- 이산확률변수 X의 기대값(분석적/수학적 평균): E[X] = \sum_{i=1}^{n} x_i * f(x_i) (가능한 모든 값 X_i에 대응되는 확률을 가중합한 것, n은 가능한 값의 수)
- 연속확률변수 X의 기대값: E[X] = \int_{X} x * f(x) dx (적분기호 사용)
- 경험적(Empirically) 표본 평균: \bar{x} = \sum_{i=1}^{T} x_i / T (T는 표본관측값들의 수)
- 확률변수 함수의 기대값: E[g(X)] = \sum_{i=1}^{n} g(x_i) * f(x_i)
  * 함수가 취하는 값이 달라질 뿐, 대응되는 확률 f(x_i)는 변하지 않음.
  * E[X²] = \sum x_i² * f(x_i), E[X³] = \sum x_i³ * f(x_i)
  * 예시 계산 (X가 0,1,2,3,4 이고 확률이 각 .1, .3, .3, .2, .1 일 때):
    E[X] = 0(.1)+1(.3)+2(.3)+3(.2)+4(.1) = 1.9
    E[X²] = 0(.1)+1(.3)+4(.3)+9(.2)+16(.1) = 4.9
    E[X³] = 0(.1)+1(.3)+8(.3)+27(.2)+64(.1) = 14.5
  * 선형성: g(X) = g₁(X) + g₂(X) 이면 E[g(X)] = E[g₁(X)] + E[g₂(X)]

■ 분산(Variance)과 표준편차
- 정의: var(X) = E[(X - E[X])²] (기대값을 중심으로 X가 취하는 값의 편차의 제곱의 기대값)
- 전개식: var(X) = E[X² - 2X*E[X] + (E[X])²] = E[X²] - 2(E[X])² + (E[X])² = E[X²] - (E[X])²
- 이산확률변수의 분산: var(X) = \sum_{i=1}^{n} (x_i - E[X])² * f(x_i)
- 표준편차(standard deviation): 분산의 양의 제곱근 (√var(X))

■ 결합확률밀도함수 및 주변/조건부 확률
- 결합확률밀도함수(Joint pdf) f(x,y): 확률변수 X와 Y의 모든 가능한 값들의 쌍(pair)의 발생에 대응되는 확률을 제공.
  * 함수의 기대값: E[g(X,Y)] = \sum_{i} \sum_{j} g(x_i, y_j) * f(x_i, y_j)
  * 예시 (자가주택여부 X=0,1 및 자가용수 Y=1,2 데이터 결합확률):
    f(0,1)=.45, f(0,2)=.15, f(1,1)=.05, f(1,2)=.35 일 때,
    E(XY) = (0)(1)(.45) + (0)(2)(.15) + (1)(1)(.05) + (1)(2)(.35) = 0.75
- 한계확률밀도함수(Marginal pdf): 결합확률함수를 다른 변수의 값들에 대해 모두 합하여 구함.
  * f(x_i) = \sum_{j} f(x_i, y_j), f(y_j) = \sum_{i} f(x_i, y_j)
  * 위 예시에서 X의 한계pdf: f(X=0) = .45+.15 = .60, f(X=1) = .05+.35 = .40
  * Y의 한계pdf: f(Y=1) = .45+.05 = .50, f(Y=2) = .15+.35 = .50
- 조건부확률밀도함수(Conditional pdf):
  * Y=y로 주어졌을 때 X의 조건부pdf: f(x|y) = f(x,y) / f(y)
  * X=x로 주어졌을 때 Y의 조건부pdf: f(y|x) = f(x,y) / f(x)
  * 위 예시 계산: f(Y=1|X=0) = .45/.60 = .75, f(Y=2|X=0) = .15/.60 = .25
                  f(X=0|Y=1) = .45/.50 = .90, f(X=1|Y=1) = .05/.50 = .10
                  f(X=0|Y=2) = .15/.50 = .30, f(X=1|Y=2) = .35/.50 = .70
                  f(Y=1|X=1) = .05/.40 = .125, f(Y=2|X=1) = .35/.40 = .875

■ 독립성과 공분산, 상관관계
- 독립인 확률변수(Independent r.v.): 모든 i와 j의 쌍에 대해 결합pdf가 각각의 한계pdf의 곱으로 표시될 때. f(x_i, y_j) = f(x_i) * f(y_j)
- 공분산(Covariance): 두 확률변수 X와 Y의 선형관계의 정도를 측정.
  * cov(X,Y) = E[(X - E[X])(Y - E[Y])] = E[XY] - E[X]E[Y]
  * 분산은 공분산의 특별한 경우임: cov(X,X) = var(X)
- 상관(Correlation) 계수: 공분산을 각각의 표준편차의 곱으로 나눈 단위 없는 값 (-1과 1 사이).
  * ρ(X,Y) = cov(X,Y) / (√var(X) * √var(Y))
- 영의 공분산 및 상관: 독립인 확률변수들은 항상 0의 공분산과 0의 상관을 가짐. 하지만 그 역(역명제)은 일반적으로 성립하지 않음(공분산이 0이라고 해서 반드시 독립은 아님).

■ 확률변수 결합의 평균과 분산
- 가중합계의 평균(기대값): E도 선형작용자이므로 개별 항의 기대값들의 가중합과 같음.
  * E[c₁X + c₂Y] = c₁E[X] + c₂E[Y]
  * 일반화: E[c₁X₁ + ... + c_nX_n] = c₁E[X₁] + ... + c_nE[X_n]
- 가중합계의 분산:
  * 두 확률변수의 가중합의 분산: V(c₁X + c₂Y) = c₁²V(X) + c₂²V(Y) + 2c₁c₂Cov(X,Y)
  * 두 확률변수의 가중차의 분산: V(c₁X - c₂Y) = c₁²V(X) + c₂²V(Y) - 2c₁c₂Cov(X,Y)
  * 일반화식: V(\sum c_i X_i) = \sum_{i}\sum_{j} c_i c_j Cov(X_i, X_j) = \sum c_i² V(X_i) + \sum_{i≠j}\sum c_i c_j Cov(X_i, X_j)

■ 주요 확률분포
- 정규분포 (Normal Distribution): Y ~ N(β, σ²)
  * pdf 공식: f(y) = (1 / √(2πσ²)) * exp[ -(y - β)² / 2σ² ] (평균 β에 대해 대칭인 종모양)
- 표준정규분포: Z = (Y - β)/σ 로 표준화하면 Z ~ N(0, 1)
  * pdf 공식: f(z) = (1 / √(2π)) * exp[ -z² / 2 ]
  * 확률 계산 예: P[Y ≥ a] = P[(Y-β)/σ ≥ (a-β)/σ] = P[Z ≥ (a-β)/σ]
  * 확률 구간 예: P[a ≤ Y ≤ b] = P[(a-β)/σ ≤ Z ≤ (b-β)/σ]
  * 정규분포의 선형결합: 정규분포를 하는 확률변수들의 선형결합도 정규분포를 따름.
    Y_i ~ N(β_i, σ_i²) 이고 W = c₁Y₁ + ... + c_nY_n 이면, W ~ N[E(W), var(W)] 임.
- 카이제곱 분포 (Chi-square Distribution):
  * Z₁, ..., Z_m이 m개의 독립인 N(0,1) 표준정규확률변수들일 때, 그 제곱합 V ≡ Z₁² + ... + Z_m² 은 자유도가 m인 카이제곱분포를 따름. V ~ χ²_(m)
  * 평균: E[V] = m, 분산: var[V] = 2m
- t분포 (Student-t Distribution):
  * Z ~ N(0,1) 이고 V ~ χ²_(m) 이며, Z와 V가 서로 독립일 때, t ≡ Z / √(V/m) 은 자유도가 m인 t분포를 따름. t ~ t_(m)
  * 평균: E[t] = 0 (m > 1일 때 존재하며, 0에 대해 대칭임)
  * 분산: var[t] = m / (m - 2) (m > 2일 때 존재)
- F분포 (F Distribution):
  * V₁ ~ χ²_(m₁) 이고 V₂ ~ χ²_(m₂) 이며, V₁과 V₂가 서로 독립일 때, 두 카이제곱변수를 각각의 자유도로 나눈 값의 비율 F ≡ (V₁/m₁) / (V₂/m₂) 는 분자 자유도 m₁, 분모 자유도 m₂인 F분포를 따름. F ~ F_(m₁, m₂)
  * 성질: F ~ F_{m,n} 이면 역수는 1/F ~ F_{n,m} 이 성립함. 또한 t-분포 변수의 제곱은 분자 자유도가 1인 F분포를 따름: t_m² ~ F_{1,m}
